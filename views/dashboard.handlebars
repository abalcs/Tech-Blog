
    <div class="mb-5 pt-3">
        <a href="/dashboard/create/"><button id="create-post" class="btn-info ms-2 p-1 rounded" style="width: 100px;">create post</button></a>
        
        <h2 class="title pb-4 mt-4 ms-2">Your posts</h2><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="button mb-5 ml-5 is-warning">edit post</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic ms-2">you do not currently have any posts :(</h6>

{{/if}}
