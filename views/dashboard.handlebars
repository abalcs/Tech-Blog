
    <div class="mb-4 pt-3">
        <h2 class="title pb-4 mt-4 ms-2">Your posts</h2><hr>
        <a href="/dashboard/create/"><button id="create-post" class="btn-info ms-2 p-1 rounded" style="width: 100px; color: white;">create post</button></a>
        
{{#if posts.length}}
    </div>
        <div>
            {{#each posts as |post|}}
            {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="btn-info ms-2 p-1 rounded mt-3" style="width: 100px; color: white;">Edit post</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="ms-2 mt-2" style="font-weight: bold;">you do not currently have any posts :(</h6>

{{/if}}
